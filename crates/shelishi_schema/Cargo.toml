[package]
name = "shelishi_schema"
version.workspace = true
edition = "2021"

[lib]
name = "shelishi_schema"
path = "src/lib.rs"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
syon-parser = { path = "../syon-parser" }
sheni_types = { path = "../sheni_types" }

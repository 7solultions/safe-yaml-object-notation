[package]
name = "hodesh_calendar"
version.workspace = true
edition = "2021"

[lib]
name = "hodesh_calendar"
path = "src/lib.rs"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
luach_types = { path = "../luach_types" }
